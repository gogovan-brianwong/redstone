# from channels.routing import route, route_class
# from UI.dashboard import consumers
# from UI.dashboard import const

channel_routing = [

    # consumers.DashboardConsumer.as_route(path="^/1/", attrs={'group': const.GROUP_NAME[0], 'group_prefix': 'internal'}),
    # consumers.DashboardConsumer.as_route(path=r"^/dashboard/"),

    #
    # route('http.request', http_consumer),
    # consumers.PodTerminal.as_route(path="r^/pods/status/$"),
    # consumers.Dashboard.as_route(path="r^/common/dashboard/$")
    # route('websocket.connect', consumers.ws_connect),
    # route('websocket.disconnect', consumers.ws_disconnect),
    # route('websocket.receive', consumers.ws_receive),
    # route_class( consumers.Dashboard,path=r"^/common/dashboard/$"),
    # route_class( consumers.PodTerminal, path=r"^/pods/status/$"),


]